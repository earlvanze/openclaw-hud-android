# Play Screenshots

Use `scripts/capture-play-screenshots.sh` to generate deterministic phone
screenshots for Google Play review/demo mode. Generated PNG files are ignored by
git; after uploading them in Play Console, record their names or URLs in
`play/app-content-answers.json` under `finalSubmission.phoneScreenshots`.
